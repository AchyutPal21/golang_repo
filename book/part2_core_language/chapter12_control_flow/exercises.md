# Chapter 12 — Exercises

## 12.1 — FizzBuzz with a single switch

Run [`exercises/01_fizzbuzz_switch`](exercises/01_fizzbuzz_switch/main.go).
Read the implementation; note the order of cases (most-specific first).

## 12.2 — First negative in a matrix

Already in `examples/03_labels_goto/main.go`. Modify it to find the
*last* negative element instead of the first. Hint: don't return early.

## 12.3 ★ — Retry loop conversion

Take the `tryWithGoto` function and rewrite it using only `for`,
`break`, and `continue`. Compare readability. Which form do you
prefer, and why?
